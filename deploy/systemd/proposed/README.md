# PROPOSED systemd hardening — NOT YET APPLIED

Generated by the 2026-06-14 supervision audit
(`docs/process-supervision-inventory-20260614.md`). **Nothing here is live.**

These are resource-limit / OOM drop-ins and one unit refactor. Apply per the
"Ordered apply plan" in the audit doc, one at a time, with the verify/rollback
commands shown there.

- `*-limits.conf` files are drop-ins. Install as
  `/etc/systemd/system/<unit>.d/limits.conf` then `systemctl daemon-reload`.
  The base unit is never edited, so rollback = delete the drop-in + reload.
- `sub-updater.service` + `sub-updater.timer` REPLACE the current
  while-loop unit on RU (oneshot + timer). Keep the old unit in git/backup for
  rollback.

Limits are sized from RSS observed during the audit (see the doc's table).
Adjust if real RSS has grown.
