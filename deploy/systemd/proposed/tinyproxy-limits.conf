# PROPOSED drop-in — FIN — install as /etc/systemd/system/tinyproxy.service.d/limits.conf
# Rationale: Telegram egress proxy (RU bots tunnel through it), RSS ~3 MB.
# Lifeline for bot connectivity -> negative OOM.
[Service]
MemoryHigh=32M
MemoryMax=64M
CPUQuota=30%
OOMScoreAdjust=-100
