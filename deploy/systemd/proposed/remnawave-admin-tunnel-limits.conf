# PROPOSED drop-in — RU — install as /etc/systemd/system/remnawave-admin-tunnel.service.d/limits.conf
# Rationale: ssh -N tunnel to SWE admin (127.0.0.1:31800), RSS ~2 MB.
[Service]
MemoryHigh=24M
MemoryMax=48M
CPUQuota=15%
OOMScoreAdjust=-100
